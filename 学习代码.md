# 嵌入模型触发时间

1. 离线/导入阶段（数据准备时）
   • 触发动作：你在前端点击"上传知识库文件"并确认上传。
   • 代码位置：对应你代码中 VectorStoreService 被调用时。
   • 逻辑：这是"一次性"的计算。模型一次性处理完所有文本，存入你的本地向量库（如 Chroma）。之后只要不更新知识库，这一步就不会再触发。
2. 在线/推理阶段（用户提问时）
   • 触发动作：用户在 st.chat_input 输入问题并回车。
   • 逻辑流转：
   1. RagService 接收到 input。
   2. 触发嵌入模型：立刻将这个 input 向量化。
   3. 检索：拿着这个向量去库里做相似度比对。
   4. 返回结果：把找到的法条（Context）喂给通义千问（LLM）。

---

# Streamlit Session State 存储总览

> Streamlit 的 `st.session_state` 是一个**按浏览器标签页隔离**的字典，每个用户打开一个页面就有独立的一份。
> 页面每次交互（点按钮、输入文字、URL 变化等）都会触发脚本**从头到尾重新执行**，而 `session_state` 在重跑之间**保持不变**，是 Streamlit 维持状态的核心机制。

## 一览表

| 键名 | 类型 | 初始化位置（行号） | 作用 |
|------|------|---------------------|------|
| `session_ids` | `list[str]` | L731-733 | 所有会话 ID 的列表 |
| `active_session_id` | `str` | L735-736 | 当前正在使用的会话 ID |
| `message` | `list[dict]` | L756-757 | 当前会话的聊天消息列表 |
| `rag` | `RagService` | L759-760 | RAG 服务实例（单例） |
| `show_uploader` | `bool` | L768-769 | 文件上传面板是否展开 |
| `kb_service` | `KnowledgeBaseService` | L841-842 | 知识库上传服务实例（懒加载单例） |

---

## 逐项详解

### 1. `session_ids` — 会话 ID 列表

```python
# L731-733
if "session_ids" not in st.session_state:
    sessions = list_session_ids()          # 从磁盘读取已有会话
    st.session_state["session_ids"] = sessions or [new_session_id()]
```

- **类型**：`list[str]`，每个元素形如 `"chat_a1b2c3d4e5f6"`
- **来源**：`list_session_ids()` 扫描 `chat_history/` 目录下已持久化的会话文件
- **用途**：渲染侧边栏的历史会话列表，判断会话是否存在
- **更新时机**：
  - 初始化时从磁盘加载（L731-733）
  - 每次页面重跑时用磁盘最新数据刷新（L744-747）
  - 新建/删除会话时通过 `_consume_query_action()` 间接更新

### 2. `active_session_id` — 当前活跃会话 ID

```python
# L735-736
if "active_session_id" not in st.session_state:
    st.session_state["active_session_id"] = st.session_state["session_ids"][0]
```

- **类型**：`str`
- **用途**：
  - 标识当前对话属于哪个会话，决定聊天记录读写到哪个文件
  - 传给 `config.build_session_config()` 生成 LangChain 的 `configurable` 配置（L754）
  - 在侧边栏中高亮显示当前会话（active 样式）
- **更新时机**：通过 URL 动作分发更新——
  - `?action=new`：创建新会话并切换（L82）
  - `?action=open&sid=xxx`：切换到指定会话（L85）
  - `?action=delete&sid=xxx`：删除后自动切到剩余会话（L92-97）

### 3. `message` — 当前会话的消息列表

```python
# L756-757
if "message" not in st.session_state:
    st.session_state["message"] = load_messages_from_history(
        st.session_state["active_session_id"]
    )
```

- **类型**：`list[dict[str, str]]`，每个元素格式为 `{"role": "user"/"assistant", "content": "..."}`
- **用途**：
  - 前端循环渲染所有聊天气泡（L763-764）
  - 用户发消息时追加 user 条目（L854）
  - 模型回复完毕后追加 assistant 条目（L945）
  - 文件上传结果也作为 assistant 消息追加（L845-848）
- **与后端历史的关系**：
  - `message` 是 **UI 展示层** 的消息列表
  - 后端 `history_store` 中还有 `type=system` 的内部摘要消息（用于记忆压缩），这些在 `load_messages_from_history()` 中被过滤掉了（L48-52），**不会出现在 session_state 里**
- **默认值**：如果会话无历史，返回一条欢迎语（L34）：
  ```python
  [{"role": "assistant", "content": "你好，我是一个法律问答助手，我有什么可以帮助你？"}]
  ```

### 4. `rag` — RAG 服务实例

```python
# L759-760
if "rag" not in st.session_state:
    st.session_state["rag"] = RagService()
```

- **类型**：`RagService` 实例
- **用途**：封装了整个 RAG 链（检索 + 生成），通过 `st.session_state["rag"].chain` 拿到 LangChain 的可调用 chain（L925）
- **为什么放在 session_state**：`RagService()` 初始化较重（加载模型、连接向量库等），放在 session_state 中可以确保**每个用户标签页只初始化一次**，后续重跑脚本时复用
- **生命周期**：与浏览器标签页同生共死，关闭页面即销毁

### 5. `show_uploader` — 文件上传面板开关

```python
# L768-769
if "show_uploader" not in st.session_state:
    st.session_state["show_uploader"] = False
```

- **类型**：`bool`
- **用途**：控制知识库文件上传区域的显示/隐藏（L831）
- **切换方式**：用户点击输入框旁的 `+` / `−` 按钮，通过 URL 参数 `?toggle_upload=1` 触发取反（L772-776）
- **自动关闭**：上传文件完成后自动设为 `False`（L849）

### 6. `kb_service` — 知识库上传服务（懒加载）

```python
# L841-842
if "kb_service" not in st.session_state:
    st.session_state["kb_service"] = KnowledgeBaseService()
```

- **类型**：`KnowledgeBaseService` 实例
- **用途**：处理上传的 TXT 文件 → 预处理 → 分块 → 向量化 → 存入 Chroma
- **懒加载**：只有在用户**实际上传文件时**才创建（不像 `rag` 在页面加载时就初始化），因为大部分对话不需要上传功能

---

## 数据流示意

```
用户打开页面
    │
    ▼
┌──────────────────────────────────────────┐
│  初始化阶段（仅首次）                       │
│  session_ids ← 磁盘会话列表                │
│  active_session_id ← 第一个会话 ID         │
│  message ← 从历史加载（过滤内部摘要）        │
│  rag ← RagService()                      │
│  show_uploader ← False                   │
└──────────────┬───────────────────────────┘
               │
    ▼──────────┴──────────▼
  侧边栏操作              聊天输入
  (URL query_params)     (st.chat_input)
    │                        │
    ▼                        ▼
  _consume_query_action()   message.append(user_msg)
  更新 active_session_id    rag.chain.stream(input)
  更新 message              message.append(ai_msg)
  st.rerun()               渲染气泡
```

---

## 关键设计点

1. **UI 消息 vs 后端历史分离**：`session_state["message"]` 只存展示用的消息，后端 `history_store` 还维护着 system 类型的摘要消息（用于记忆压缩），两者通过 `load_messages_from_history()` 的过滤逻辑衔接。

2. **重量级对象单例化**：`rag` 和 `kb_service` 都通过 `if key not in session_state` 模式实现单例，避免 Streamlit 每次重跑脚本时反复创建昂贵的对象。

3. **URL 驱动的动作分发**：侧边栏的新建/切换/删除/置顶操作不依赖 Streamlit 按钮组件，而是通过 `<a href="?action=xxx">` 链接 + `_consume_query_action()` 解析 URL 参数来实现，这样可以在纯 HTML 侧边栏中使用自定义样式。
