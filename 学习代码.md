# 嵌入模型触发时间

1. 离线/导入阶段（数据准备时）
   • 触发动作：你在前端点击"上传知识库文件"并确认上传。
   • 代码位置：对应你代码中 VectorStoreService 被调用时。
   • 逻辑：这是"一次性"的计算。模型一次性处理完所有文本，存入你的本地向量库（如 Chroma）。之后只要不更新知识库，这一步就不会再触发。
2. 在线/推理阶段（用户提问时）
   • 触发动作：用户在 st.chat_input 输入问题并回车。
   • 逻辑流转：
   1. RagService 接收到 input。
   2. 触发嵌入模型：立刻将这个 input 向量化。
   3. 检索：拿着这个向量去库里做相似度比对。
   4. 返回结果：把找到的法条（Context）喂给通义千问（LLM）。

---

# Streamlit Session State 存储总览

> Streamlit 的 `st.session_state` 是一个**按浏览器标签页隔离**的字典，每个用户打开一个页面就有独立的一份。
> 页面每次交互（点按钮、输入文字、URL 变化等）都会触发脚本**从头到尾重新执行**，而 `session_state` 在重跑之间**保持不变**，是 Streamlit 维持状态的核心机制。

## 一览表

| 键名 | 类型 | 初始化位置（行号） | 作用 |
|------|------|---------------------|------|
| `session_ids` | `list[str]` | L731-733 | 所有会话 ID 的列表 |
| `active_session_id` | `str` | L735-736 | 当前正在使用的会话 ID |
| `message` | `list[dict]` | L756-757 | 当前会话的聊天消息列表 |
| `rag` | `RagService` | L759-760 | RAG 服务实例（单例） |
| `show_uploader` | `bool` | L768-769 | 文件上传面板是否展开 |
| `kb_service` | `KnowledgeBaseService` | L841-842 | 知识库上传服务实例（懒加载单例） |


## 数据流示意

```
用户打开页面
    │
    ▼
┌──────────────────────────────────────────┐
│  初始化阶段（仅首次）                       │
│  session_ids ← 磁盘会话列表                │
│  active_session_id ← 第一个会话 ID         │
│  message ← 从历史加载（过滤内部摘要）        │
│  rag ← RagService()                      │
│  show_uploader ← False                   │
└──────────────┬───────────────────────────┘
               │
    ▼──────────┴──────────▼
  侧边栏操作              聊天输入
  (URL query_params)     (st.chat_input)
    │                        │
    ▼                        ▼
  _consume_query_action()   message.append(user_msg)
  更新 active_session_id    rag.chain.stream(input)
  更新 message              message.append(ai_msg)
  st.rerun()               渲染气泡
```

---

## 关键设计点

1. **UI 消息 vs 后端历史分离**：`session_state["message"]` 只存展示用的消息，后端 `history_store` 还维护着 system 类型的摘要消息（用于记忆压缩），两者通过 `load_messages_from_history()` 的过滤逻辑衔接。

2. **重量级对象单例化**：`rag` 和 `kb_service` 都通过 `if key not in session_state` 模式实现单例，避免 Streamlit 每次重跑脚本时反复创建昂贵的对象。

3. **URL 驱动的动作分发**：侧边栏的新建/切换/删除/置顶操作不依赖 Streamlit 按钮组件，而是通过 `<a href="?action=xxx">` 链接 + `_consume_query_action()` 解析 URL 参数来实现，这样可以在纯 HTML 侧边栏中使用自定义样式。

# Agent 处理一次用户问题，大概分成 6 步：

1. 接收用户问题和历史对话。
2. 先做本地 RAG 检索，拿到法律知识库里的相关证据。
3. 加载 MCP 工具列表，把可用工具描述放进 system prompt。
4. 构造消息上下文：系统提示词 + 历史记忆 + 当前问题。
5. 调用大模型，让模型决定是直接回答，还是调用外部工具。
6. 如果模型调用工具，就执行工具，把结果作为 Observation 放回上下文，再让模型继续推理，直到生成最终答案。
可以概括成：
```
User Question
    ↓
Hybrid Retrieval 本地知识库检索
    ↓
Build Prompt：本地证据 + MCP 工具列表 + 历史记忆
    ↓
LLM Reasoning
    ↓
是否需要工具？
    ├── 不需要：Final Answer
    └── 需要：Tool Call
              ↓
           MCP Tool 执行
              ↓
           ToolMessage / Observation
              ↓
           LLM 再推理
              ↓
           Final Answer
```

# RAG 和 ReAct 是怎么结合的

项目不是纯 ReAct，而是 RAG + ReAct Agent。

在 _run_agent 里，第一步不是直接让模型回答，而是先检索本地知识库：

local_docs = self.hybrid_retriever.retrieve(question)
local_context = self._format_document(local_docs)
也就是说，用户问法律问题时，Agent 会先从本地法律文本库里找相关条文或证据。这个部分属于 RAG。

然后 _build_system_prompt(local_context, tool_catalog) 会把两类信息一起塞进系统提示词：

本地检索到的法律依据。
当前可用的 MCP 外部工具列表。
这样模型在推理时就知道：

本地知识库里有哪些证据；
如果本地信息不够，或者问题需要外部信息，可以调用哪些工具。
这就是项目里 RAG 和 ReAct 的结合点。

# 工具是怎么绑定给模型的

在 _build_tool_model 里，项目会判断当前有没有 MCP 工具：
如果没有工具，就退化成普通 RAG 问答。
如果有工具，就通过 bind_tools 把工具绑定到模型上。
这里的工具不是很多个 MCP 工具直接绑定进去，而是封装成了一个统一入口：

@tool("mcp_call_tool")
def mcp_call_tool(server_name, tool_name, arguments):
    return executor.execute(server_name, tool_name, arguments or {})
也就是说，大模型只需要调用一个统一工具 mcp_call_tool，然后通过参数指定：

{
  "server_name": "web-search",
  "tool_name": "search",
  "arguments": {
    "query": "..."
  }
}
这个设计的好处是工具体系比较灵活。以后新增 MCP Server，不一定要改 Agent 主流程，只要 MCP Server 能被发现并描述出来，Agent 就可以通过统一入口调用。

# ReAct 循环是怎么跑的

我项目里的 ReAct Agent 是一个 RAG + MCP Tool Calling 的 Agent。它的核心思想是让模型不是一次性回答，而是在回答过程中自己判断是否需要调用工具。

具体实现上，用户问题进入系统后，我会先通过混合检索从本地法律知识库拿到相关文档，这部分是 RAG。然后我会加载 MCP Server 暴露出来的工具列表，把本地检索结果和工具描述一起放进 system prompt。接着调用大模型。

如果模型认为已有上下文足够，它会直接生成最终答案；如果它认为需要外部信息，就会返回 LangChain 的 tool_calls。我的代码会解析这些 tool_calls，通过 MCPToolExecutor 调用对应的 MCP 工具，把工具返回结果包装成 ToolMessage 再追加回 messages。这个 ToolMessage 就相当于 ReAct 里的 Observation。然后再次调用模型，让模型基于观察结果继续推理，直到没有新的工具调用，输出最终答案。

所以它不是传统字符串格式的 Thought/Action/Observation，而是工程化的函数调用版 ReAct。Reasoning 由模型隐式完成，Action 是结构化 tool_call，Observation 是 ToolMessage，循环由 _run_agent 控制，并且我设置了最大轮数，防止无限调用工具。

这个设计的好处是：本地知识库可以保证法律回答有依据，MCP 工具可以扩展外部能力，ReAct 循环可以让模型动态决定是否查工具，同时系统还有降级机制，即使工具不可用，也能退回普通 RAG 问答。

MCP 工具调用链路

LLM tool_call
    ↓
RagService._parse_tool_call
    ↓
MCPToolExecutor.execute
    ↓
MCPClientManager.call_tool_sync
    ↓
MCP Server stdio session
    ↓
session.call_tool(...)
    ↓
返回工具结果
    ↓
ToolMessage 回填给 LLM
MCPClientManager 会从配置里加载 MCP Server，例如：

{
  "servers": [
    {
      "name": "web-search",
      "command": "python",
      "args": ["servers/web_search_mcp.py"],
      "transport": "stdio"
    }
  ]
}
然后它可以做两件事：

list_tools：列出 MCP Server 提供了哪些工具。
call_tool：真正调用某个工具。
describe_tools_text 会把工具名称、描述、参数 schema 转成文本，放进 prompt，让模型知道有哪些工具可以用。

# 面试中可以强调的设计亮点

第一，Agent 有降级能力。

如果没有 MCP 工具，或者 bind_tools 失败，系统会退回普通 RAG 模型：

return self.chat_model
这样不会因为工具系统异常导致整个问答不可用。

第二，工具调用是结构化的。

项目没有靠正则解析 Action: xxx，而是用 LangChain 的 tool_calls，稳定性更好，也更适合生产环境。

第三，RAG 和工具调用结合。

本地法律知识库能解决大部分稳定知识问题，MCP 工具适合补充外部实时信息或扩展能力。

第四，有最大轮数控制。

避免 Agent 陷入无限工具调用。

第五，有历史记忆接入。

_build_chain 里用了 RunnableWithMessageHistory，通过 get_history 加载会话历史，所以 Agent 不只是单轮问答，也能在多轮对话里延续上下文。

# 为什么不用现成 create_react_agent？

主要是为了更可控。现成 ReAct Agent 通常封装程度比较高，而我这个项目需要把本地 RAG 检索、会话历史压缩、MCP 工具发现、工具调用异常处理、最大调用轮数这些逻辑组合起来。自己实现循环后，我可以明确控制 prompt 结构、工具执行链路、失败降级和最终输出格式。

另外我这里使用的是模型原生 tool calling，不需要模型严格遵守 Thought/Action/Observation 文本格式，减少了解析失败的风险，更适合工程落地。

# 滑动窗口：
保留最近 3 轮对话
```
[Human, AI, Human, AI, Human, AI]
  └─轮1──┘  └─轮2──┘  └─轮3──┘
```
按 HumanMessage 分轮 → 取最后 N 轮 → 旧轮送去摘要。保证短期记忆连贯。
保留最近 N 轮是为了让模型拥有短期记忆连续性——用户在最近几轮中提到的上下文可以直接被模型看到，无需依赖摘要的有损压缩。
# Token 预算裁剪：

在窗口 + 摘要之后做最终兜底，
当压缩后消息仍超预算时，逐轮从旧到新删除直到 token 预算内，至少保留 1 轮 + 摘要。保证不超模型上下文窗口。